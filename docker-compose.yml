

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:8000 --workers ${GUNICORN_WORKERS:-3} --worker-class sync --keep-alive ${GUNICORN_KEEP_ALIVE:-2} --timeout ${GUNICORN_TIMEOUT:-30} --graceful-timeout ${GUNICORN_GRACEFUL_TIMEOUT:-30} --max-requests ${GUNICORN_MAX_REQUESTS:-300} --max-requests-jitter ${GUNICORN_MAX_REQUESTS_JITTER:-50} mlai.wsgi:application
    environment:
      APP_ENV: ${APP_ENV:-production}
    ports:
      - "80:8000"
    env_file:
      - .env
    depends_on:
      - db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; req=urllib.request.Request('http://127.0.0.1:8000/healthz/ready', headers={'Connection':'close'}); resp=urllib.request.urlopen(req, timeout=5); body=resp.read(); resp.close(); raise SystemExit(0 if body else 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      default:
      mlai-shared:
        aliases:
          - mlai-backend-web

  scheduler:
    build: .
    command: sh -c "until python manage.py migrate --check --noinput; do echo 'Waiting for migrations to complete...'; sleep 5; done; while true; do python manage.py run_scheduled_discovery; sleep 60; done"
    environment:
      APP_ENV: ${APP_ENV:-production}
    env_file:
      - .env
    depends_on:
      - db
    networks:
      default:
      mlai-shared:
        aliases:
          - mlai-backend-scheduler

  bridge-worker:
    build: .
    command: sh -c "until python manage.py migrate --check --noinput; do echo 'Waiting for migrations to complete...'; sleep 5; done; python manage.py run_community_bridge"
    environment:
      APP_ENV: ${APP_ENV:-production}
    env_file:
      - .env
    depends_on:
      - db
    networks:
      default:
      mlai-shared:
        aliases:
          - mlai-backend-bridge-worker
    restart: unless-stopped

  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

networks:
  mlai-shared:
    external: true

volumes:
  postgres_data:
