{%- assign topic_list = trigger.topics | default: topics | default: message_data.topics -%}
{%- assign greeting_name = trigger.first_name | default: first_name | default: message_data.first_name -%}
{%- assign site_domain = trigger.domain | default: domain | default: message_data.domain -%}
{%- assign optout_url = trigger.unsubscribe_url | default: unsubscribe_url | default: message_data.unsubscribe_url -%}
{% if greeting_name != blank %}Hi {{ greeting_name }},{% else %}Your daily research is ready.{% endif %}

We researched today's best article opportunities for {{ site_domain }}.
Choose one below, then confirm your choice to start writing.

{% if topic_list == blank %}
No topics were ready this morning. We'll try again at the next scheduled run.
{% else %}
{% for topic in topic_list %}
{{ topic.rank }}. {{ topic.display_title }}
Keyword: {{ topic.keyword }}
{% if topic.why_recommended != blank %}Why this topic: {{ topic.why_recommended }}
{% endif %}Start writing: {{ topic.confirm_url }}

{% endfor %}
{% endif %}
You're receiving this because daily research is enabled for {{ site_domain }}.
Pause these emails: {{ optout_url }}
