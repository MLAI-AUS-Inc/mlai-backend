# Generated by Django 4.2.16 on 2026-06-03 14:30
#
# Adds the two FastAPI eval-server credentials that the
# `approve_teams_for_eval` admin action populates by calling
# `POST https://eval.eliascorp.org/admin/teams`:
#
#   eval_token       — `token_hash` on the eval cluster's `teams` table
#                       (plaintext today; column name is legacy)
#   eval_team_uuid   — the eval cluster's `teams.id` UUID; participants paste
#                       this into the submission portal's "Team ID" field
#
# Both fields land in a single migration to keep history linear — they're
# logically one feature (the Approval Gate) and useless without each other.

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("generic_hackathons", "0004_generichackathonjoinrequest_and_more"),
    ]

    operations = [
        migrations.AddField(
            model_name="generichackathonteam",
            name="eval_token",
            field=models.CharField(
                blank=True,
                help_text="Token for FastAPI evaluation server",
                max_length=255,
                null=True,
            ),
        ),
        migrations.AddField(
            model_name="generichackathonteam",
            name="eval_team_uuid",
            field=models.UUIDField(
                blank=True,
                help_text="UUID from FastAPI evaluation server",
                null=True,
                unique=True,
            ),
        ),
    ]
