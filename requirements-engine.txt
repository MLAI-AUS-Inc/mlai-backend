# Watt The Hack simulation engine (scenario data ships inside this package).
# Published on PyPI: https://pypi.org/project/watt-the-hack/
# Pinned to a version so Docker re-pip-installs ONLY when this file changes. Bump the
# version on each scenario release and redeploy to pull new scenarios.
watt-the-hack==0.2.0
