# Watt The Hack simulation ENGINE CODE only. Published on PyPI:
# https://pypi.org/project/watt-the-hack/
# The sandbox reads scenarios from BASE_DIR/scenarios (views.py sets
# SCENARIOS_DATA_DIR), NOT from this package — so scenario releases go via the
# synced scenarios/ dir + redeploy. Bump this version only when the engine CODE
# changes (keep it in lock-step with the admin base image's ENGINE_VERSION).
watt-the-hack==0.2.4
